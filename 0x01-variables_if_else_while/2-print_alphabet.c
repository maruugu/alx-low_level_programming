#include <stdio.h>
/**
 * main - Enrty point
 * discription: 'the alphabet in lowercase'
 * Return: always 0
 */
int main(void)
{
int c;
for (char c = 'a' ; c <= 'z' ; c++)
{
putchar(c);
}
putchar ('\n');
return (0);
}
